
Phase 0 — Backend & Frotend & Database is working locally - DONE 
Phase 1 — Git Strategy First - DONE 
Phase 2 — Webhook Before Jenkins Pipeline - DONE 
Phase 3 — Dockerize Applications -> create docker files (Frotend,Backend,Ngnix) and run locally - DONE 
Phase 4 — Docker Compose Local -> Create Docker Compose file for (Frotend,Backend,Ngnix)) & DB is on laptop - DONE
Phase 5 — Nginx Reverse Proxy -> Created in Docker Compose itself - DONE
	Note: before pushing Images to Docker hub please make sure that you have created the repositories in docker hub also create PAT on Dockerhub and add it in jenkins creds
Phase 6 — Jenkins Build Pipeline (Code to Image Push)
	note before S3 & Dynamo we created Bootstrep to create s3 & dynamo db 
	then we only stored the terrafom.state file in s3 using dynmodb lock 
	then will create network : VPC ,IGW ,Public & Private Subnet, Route , Routetable
	then we create security : 
	then we create Iam :
	then we create RDS :
	then we create EC2 :
Phase 7 — Terraform State Infrastructure (s3 + dynamoDB) + 
Phase 8 — Terraform EC2 + EIP

Before ding anythging & after applying terformec2 creat Publish Over SSH in jenkins :
	Manage Jenkins → Plugins → Publish Over SSH
		Name: certifyhub-ec2
		Hostname: <EC2-PUBLIC-IP>
		Username: ec2-user
		Remote Directory: /home/ec2-user
		add : private key 

then create the Db Tables using scripts into RDS :
	psql -h certifyhub-dev-postgres.cj0w84ei6au8.ap-south-1.rds.amazonaws.com -U certifyhub -d certifyhub
	
	also need to use chckbox & Docker Compose based on Version installed in userdata
	
	
Phase 9 — Deployment Automation
Phase 10 — Domain & HTTPS
Phase 11 — Secrets Management
Phase 12 — Rebuild Recovery Test

*********************
1. Freeze App 
2. Git Repos
3. GitHub Webhook
4. Docker Backend
5. Docker Frontend
6. Docker Compose
7. Nginx
8. Terraform State Backend
9. Terraform EC2 + EIP
10. Jenkins Build Pipeline
11. DockerHub Push
12. Automated Deployment
13. Domain + SSL
14. Recovery Testing


terraform/
├── backend.tf
├── provider.tf
├── vpc.tf
├── security-group.tf
├── ec2.tf
├── rds.tf
├── outputs.tf
└── variables.tf


Real Enterprise Architecture:

                   Internet
                        │
                Internet Gateway
                        │
                Public Subnet
                        │
                    EC2
          ┌─────────┼─────────┐
          │         │         │
       Nginx    Frontend   Backend
                                │
                                │
                       Private Subnet
                                │
                           RDS PostgreSQL